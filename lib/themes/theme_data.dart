import 'package:flutter/material.dart';
import 'package:pokemon_pokedex_app/constants/color_constants.dart';

final darkTheme = ThemeData(
  primaryColor: blueDark,
  scaffoldBackgroundColor: blueGrey,
  appBarTheme: const AppBarTheme(backgroundColor: blueDark),
);

final lightTheme = ThemeData(
  primaryColor: redAppBar,
  scaffoldBackgroundColor: whiteYellow,
  appBarTheme: const AppBarTheme(backgroundColor: redAppBar),
);
